# Use a base image, for example, a basic Ubuntu image
FROM ubuntu:20.04

# Install a tool for sending HTTP requests (e.g., curl)
RUN apt-get update && apt-get install -y curl

# Start a shell to keep the container running
CMD ["bash"]
